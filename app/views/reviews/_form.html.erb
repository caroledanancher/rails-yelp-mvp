<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, as: :select, collection: Review::RATING, include_blank:  "Select a category" %>
  <%= f.submit "Add a review", class: "btn btn-primary" %>
<% end %>
